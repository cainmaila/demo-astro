# Astro 入門套件：最小化

```sh
pnpm create astro@latest -- --template minimal
```

> 🧑‍🚀 **經驗豐富的太空人？** 刪除此文件。玩得開心！

## 🚀 專案結構

在您的 Astro 專案中，您會看到以下資料夾和文件：

```text
/
├── public/
├── src/
│   └── pages/
│       └── index.astro
└── package.json
```

Astro 會在 `src/pages/` 目錄中尋找 `.astro` 或 `.md` 文件。每個頁面會根據其文件名作為路由公開。

`src/components/` 沒有什麼特別之處，但這是我們喜歡放置任何 Astro/React/Vue/Svelte/Preact 元件的地方。

任何靜態資源，如圖片，都可以放置在 `public/` 目錄中。

## 🧞 命令

所有命令都是從專案根目錄在終端機中運行：

| 命令                   | 動作                                         |
| :--------------------- | :------------------------------------------- |
| `pnpm install`         | 安裝依賴項                                   |
| `pnpm dev`             | 在 `localhost:4321` 啟動本地開發伺服器       |
| `pnpm build`           | 建置您的生產網站到 `./dist/`                 |
| `pnpm preview`         | 在部署前本地預覽您的建置                     |
| `pnpm astro ...`       | 運行 CLI 命令，如 `astro add`、`astro check` |
| `pnpm astro -- --help` | 獲取使用 Astro CLI 的幫助                    |

## 👀 想了解更多？

歡迎查看[我們的文檔](https://docs.astro.build)或加入我們的 [Discord 伺服器](https://astro.build/chat)。
